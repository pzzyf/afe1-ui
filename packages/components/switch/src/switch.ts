export const switchProps = {
  inlinePrompt: Boolean,
  inactiveIcon: Object,
  inactiveText: String,
  activeIcon: Object,
  activeText: String,
  loading: Boolean,
  activeActionIcon: Object,
  inactiveActionIcon: Object,
}
